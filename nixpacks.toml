[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "cd ClinicApp.Web",
  "dotnet restore",
  "dotnet publish -c Release -o out"
]

[start]
cmd = "cd ClinicApp.Web/out && dotnet ClinicApp.Web.dll"
