[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "cd ClinicApp.Web",
  "dotnet clean",
  "dotnet restore --force",
  "dotnet publish -c Release -o out --no-cache"
]

[start]
cmd = "cd ClinicApp.Web/out && dotnet ClinicApp.Web.dll"
